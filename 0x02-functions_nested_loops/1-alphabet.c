#include "main.h"
/**
 * main - Entry point
 * Initial alphabet aka "a"
 * Return: 0 (Success)
 */
int main(void)
{
	char a;

	for (a = 'a'; a <= 'z'; a++)
	_putchar(a);
	_putchar('\n');
	return (0);
}
